run the following commands in order to get Allure report
mvn clean test
mvn allure:serve

-------------------------
log file is placed in test/resources/ folder
_________________________
allure-results folder with json files is placed in  test/resources/ folder